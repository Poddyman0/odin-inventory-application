extends layout

block content
  h1= title
  p Welcome to #{title}

  p The inventory has the following record counts:

  ul
    li #[strong Items:] !{item_count}
    li #[strong Categorys:] !{category_count}