extends layout

block content
  h1= title
  if categorys_list.length
    ul
      each category in categorys_list
        li
          a(href=category.url) #{category.name}

  else
    p There are no categorys.